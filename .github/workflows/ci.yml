name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-test:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Seeder sanity (dry run)
        run: |
          cat > /tmp/sample.bib <<'EOF'
          @inproceedings{lester2021prompt,
            title={The Power of Scale for Parameter-Efficient Prompt Tuning},
            author={Lester, Brian and Al-Rfou, Rami and Constant, Noah},
            year={2021},
            booktitle={EMNLP},
            url={https://arxiv.org/abs/2104.08691}
          }
          EOF
          python scripts/bibtex_to_yaml.py /tmp/sample.bib --dry-run

      - name: Validate lists
        run: |
          python scripts/validate_lists.py

      - name: Render README (auto)
        run: |
          python scripts/render_readme.py
          # Auto-commit README changes if any
          if ! git diff --exit-code -- README.md; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add README.md
            git commit -m "Auto-update README timestamp [skip ci]"
            git push
            echo "README updated and committed automatically"
          else
            echo "README is already up to date"
          fi

      - name: Linkcheck
        run: |
          python scripts/linkcheck.py

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: awesome-lint
        run: |
          npx --yes awesome-lint README.md

      - name: markdownlint
        run: |
          npx --yes markdownlint-cli@0.39.0 "**/*.md" --ignore node_modules

      - name: MkDocs build
        run: |
          mkdocs build --strict
